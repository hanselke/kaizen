
.row.page-container
  .container-fluid
    .span3(style="margin-top:40px")
      .well
        ul.nav.nav-list
          li.nav-header
            |Process Definitions
          li.active
            |Overview
          li
            a(href="#/admin/process-definitions/add") Add a new one
      .well
        ul.nav.nav-list
          li.nav-header
            |More Admin
          li
            a(href="#/admin/users") Users
          li
            a(href="#/admin/tasks") Tasks
          li
            a(href="#/admin/roles") Roles

    .span9

      h2
        span Process Definitions&nbsp;
        a(href="#/admin/process-definitions/add",style="font-weight:normal;font-size:smaller") Add a new one

      table.table-striped.table-hover
        thead
          tr
            th Name
            th Actions
        tbody
          tr(style="vertical-align:center;",ng-repeat='processDefinition in processDefinitions')
            td
              |{{processDefinition.name}}
            td
              button.btn(ng-click="edit(processDefinition._id)") Edit
              button.btn(ng-click="editForm(processDefinition._id)") Upload Excel
              button.btn.btn-warning(ng-click="deleteMe(processDefinition._id)") Delete

        